# Handoff — `feat/morning-dashboard` (2026-06-01)

Status of the branch after implementing the greeting/frame redesign and Phases
2.8 → 5. **73 tests pass (`npm test`), `npm run build` is clean.**

## What shipped this session

### Header + frame redesign
- **`DashboardHeader`** — unified glass card: greeting (morning/afternoon/**night**, `Dre` in amber), date, a decorative concentric **CSS dot-ring clock** (rings spin continuously; the digital readout carries the real time), and the LUMIX briefing slot. The old inline App greeting is gone.
- **Clock widget removed** from the registry and `src/widgets/Clock/` deleted — the header owns the clock. (Plan Task 5.4 "concentric clock widget" is therefore obsolete.)
- **`WidgetFrame`** — bold uppercase accent title, dotted-matrix divider, neutral body, optional expand/remove buttons. Divider color uses `currentColor` via the accent text class (no hex duplicated into CSS).

### Phase 2.8 — GitHub contributions heatmap
- `contributionsSource.ts` + `ContributionHeatmap.tsx`, fetched lazily only when the GitHub widget is **expanded**. Dev proxy `/api/contributions` in `vite.config.ts` keeps `GITHUB_TOKEN` server-side (username `its-dreOwO`).

### Phase 3 — Animation & interactivity
- `AnimatedNumber` count-ups (GitHub/GCP/Mail key numbers); entrance/hover motion on grid items; `ExpandOverlay` click-to-expand; `AddWidgetMenu` + remove button with a persisted `dashboard.dismissed` set so removals survive reload.

### Phase 4 — LUMIX AI
- `DashboardDataContext` (content-compare guard to avoid an infinite update loop), `lib/openrouter.ts` (`fetchLUMIXBriefing`/`askLUMIX`, model `deepseek/deepseek-v4-flash`), `LumixBriefing` (header slot), `ApiKeyPopover` (gear; key in `dashboard.openrouter_api_key`, owned by `App`), `CopilotSidebar` (chat drawer opened by "Open Chat").

### Phase 5 — Visual
- `DotMatrixBackground` canvas backdrop (respects `prefers-reduced-motion`); animated Weather condition SVGs (rain/storm/cloud/sun).

## Deviations from the written plan
- Registry change was a **one-block deletion** (kept `defineWidget`, no `as` casts; left all accents untouched) rather than the plan's full rewrite.
- LUMIX UI = `LumixBriefing` + `ApiKeyPopover` (gear popover for the key) instead of the plan's `AiBriefingCard`; briefing lives in the header slot, not a separate card.
- DotMatrix **wave-settings sliders were skipped** to keep the redesigned layout clean (easy to add back).
- Clock animation uses the spec's CSS rotation, not the plan's per-second JS `strokeDashoffset`.

## Known follow-ups / not done
- **`GITHUB_USER` is still `octocat`** in `src/widgets/GitHub/GitHubWidget.tsx` (sparkline). The heatmap proxy already uses `its-dreOwO`; align these.
- **Phase 3.5 README task** not done.
- **Lint**: `npm run lint` reports ~10 errors, all the pre-existing `react-refresh/only-export-components` pattern (every widget file + the new context/grid export a component **and** a hook/helper from one file). Lint is **not** a passing gate in this repo today; the build (`tsc -b`) is. No genuine new issues remain.
- Briefing fetches once ~600ms after a key is set, using whatever widget data has loaded; there's no manual "refresh briefing" control yet.

## How to run
```bash
npm run dev            # http://localhost:5173
npm test               # 73 tests
npm run build          # tsc -b && vite build — the real typecheck gate
# For the GitHub heatmap: cp .env.example .env and set GITHUB_TOKEN
# For LUMIX: open the gear in the header and paste an OpenRouter API key
```
